import { atom } from "recoil";

export const spaceIdState = atom({
  key: "spaceIdState",
  default: " ",
});

export const SpaceCommentModalState = atom({
  key: "SpaceCommentModalState",
  default: false,
});
